import { connect } from 'react-redux';
import Banner from './banner';

const mSTP = state => {
  return {
  };
};

const mDTP = dispatch => {
  return {
  };
};

export default connect(mSTP, mDTP)(Banner);